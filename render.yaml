services:
  - type: worker
    name: telegram-bot-worker
    env: python
    plan: free
    branch: main
    repo: https://github.com/your-username/your-repo-name
    startCommand: ./start.sh
    # Здесь можно оставить пустой блок envVars, 
    # вы всё равно задаёте их в UI
    envVars: []

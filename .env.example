# --- START OF FILE .env.example ---
# Telegram Bot Token from @BotFather
BOT_TOKEN="YOUR_BOT_TOKEN"

# Database connection URL (e.g., PostgreSQL)
# Format: postgresql+asyncpg://user:password@host:port/dbname
DATABASE_URL="postgresql+asyncpg://user:password@host:port/dbname"

# Your Telegram User ID for admin commands
ADMIN_CHAT_ID="YOUR_TELEGRAM_USER_ID"

# API Key for OpenRouter.ai
OPENROUTER_API_KEY="YOUR_OPENROUTER_API_KEY"

# --- Settings for Webhook (used by main.py and config.py) ---

# Optional: Hostname for the webhook.
# Leave BLANK if deploying on Render.com (it uses RENDER_EXTERNAL_HOSTNAME)
# Set your public domain/IP if deploying elsewhere (e.g., mybot.example.com)
WEBHOOK_HOST=""

# Required: Path for the webhook URL (must start with /)
# Example: /webhook or /bot/YOUR_BOT_TOKEN
WEBHOOK_PATH="/webhook"

# Port the web application will listen on inside the container/server
# Render often expects 10000 or 8000. Check your Render service settings.
PORT="8000"

# Optional: Webhook secret token for extra security
# WEBHOOK_SECRET="your_very_strong_secret_key_here"

# --- END OF FILE .env.example ---
